<h2><%= @restaurant.name %></h2>

<p><%= @restaurant.name %> Reviews</p>

<ul>
  <% @restaurant.reviews.each do |review| %>
    <li>
      <%= review.content %>
      <p>rating: <%= review.rating %> </p>
    </li>
  <% end %>
</ul>


<%= link_to 'Back', restaurants_path %> |
<%= link_to 'Leave a review', new_restaurant_review_path(@restaurant) %>
